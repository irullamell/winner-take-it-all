# Gemfile — declare Ruby gems for Netlify Jekyll build

source "https://rubygems.org"

# Pin Ruby version to match Netlify environment
ruby "3.3.0"

# Core site generator
gem "jekyll", "~> 4.3"

# Needed only for `jekyll serve` in local dev (Netlify calls `jekyll build`)
gem "webrick", "~> 1.8"

# Uncomment / add Jekyll plugins as you need, for example:
# gem "jekyll-feed", "~> 0.17"
# gem "jekyll-sitemap", "~> 1.4"
